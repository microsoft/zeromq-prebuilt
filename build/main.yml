jobs:
- job:
  displayName: win_64
  pool:
    vmImage: 'windows-latest'
  steps:
    - template: build.yml

- job:
  displayName: win32
  pool:
    vmImage: 'windows-latest'
  steps:
    - template: build.yml
      parameters:
        python_arch: x86
